import 'package:flutter/material.dart';

class UbicacionesPages extends StatelessWidget {
  const UbicacionesPages({super.key});

  @override
  Widget build(BuildContext context) {
    // final size = MediaQuery.of(context).size;
    return Container(
      // color: Colors.red,
      child: const Center(
        child: Text(
          'ubicaciones',
          style: TextStyle(color: Colors.black),
        ),
      ),
    );
  }
}
