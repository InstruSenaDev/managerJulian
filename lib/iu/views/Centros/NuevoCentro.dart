import 'package:flutter/material.dart';

class NuevoCentro extends StatelessWidget {
  const NuevoCentro({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('NuevoCentro'),
      ),
    );
  }
}
